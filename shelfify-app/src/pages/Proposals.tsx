import React from 'react';
import Typography from '@mui/material/Typography';

const Proposals: React.FC = () => {
  return (
    <Typography variant="h4">My Proposals Page</Typography>
  );
};

export default Proposals;
