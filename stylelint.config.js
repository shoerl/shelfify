export default {
  extends: ['stylelint-config-standard'],
  rules: {
    // Add any custom rules here if needed in the future
  },
  // Specify files to lint, can be overridden by CLI arguments
  files: ['**/*.css'], 
};
